query SingleCharacterData($id :ID!) {
    character(id :$id){
        id
        name
        species
        type
        gender
        origin {
            name
            dimension
        }
        location{
            name
            dimension
        }
    }
}